# Nona-me Sales V4
Adds KHR + USD sales, configurable exchange rate, daily report export to PNG and TXT, copy/share text for Telegram, plus prior Staff/Admin prototype features.
Demo: staff01 / 1234 ; admin / admin

Important: browser LocalStorage is still used for this prototype. Production should use Supabase Auth + Database.
