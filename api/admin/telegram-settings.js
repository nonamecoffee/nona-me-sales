const SUPABASE_URL = process.env.SUPABASE_URL || 'https://aucwgnbhcbcovmshjmuo.supabase.co';
const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY || 'sb_publishable_XKT_TSLgOd75bhapXEUZ2Q_rs98dwcO';
const SERVICE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY || '';
function json(res,status,body){return res.status(status).setHeader('content-type','application/json; charset=utf-8').setHeader('cache-control','no-store').json(body)}
const clean=v=>String(v??'').trim();
async function rest(path,opts={}){const res=await fetch(`${SUPABASE_URL}${path}`,{...opts,headers:{apikey:SERVICE_KEY,authorization:'Bearer '+SERVICE_KEY,'content-type':'application/json',...(opts.headers||{})}});const raw=await res.text();let data=null;try{data=raw?JSON.parse(raw):null}catch{}return {res,data,raw}}
async function authUser(token){const r=await fetch(`${SUPABASE_URL}/auth/v1/user`,{headers:{apikey:SUPABASE_ANON_KEY,authorization:'Bearer '+token}});const d=await r.json().catch(()=>null);return r.ok?d:null}
async function requireAdmin(token,businessId){const u=await authUser(token);if(!u?.id)return null;const m=await rest(`/rest/v1/nona_me_memberships?business_id=eq.${encodeURIComponent(businessId)}&user_id=eq.${encodeURIComponent(u.id)}&active=eq.true&select=role`);if(!m.res.ok||!m.data?.length||!['owner','admin'].includes(m.data[0].role))return null;return u}
export default async function handler(req,res){if(!SERVICE_KEY)return json(res,500,{ok:false,error:'SUPABASE_SERVICE_ROLE_KEY is not configured.'});const token=clean(req.headers.authorization||'').replace(/^Bearer\s+/i,'');try{if(req.method==='GET'){const businessId=clean(req.query?.businessId);if(!businessId||!await requireAdmin(token,businessId))return json(res,403,{ok:false,error:'Not authorized.'});const r=await rest(`/rest/v1/nona_me_integrations?business_id=eq.${encodeURIComponent(businessId)}&select=telegram_bot_token,telegram_chat_id`);const row=r.data?.[0]||{};const t=clean(row.telegram_bot_token);return json(res,200,{ok:true,settings:{configured:Boolean(t&&row.telegram_chat_id),chatId:clean(row.telegram_chat_id),maskedToken:t?'••••••••••••••••':''}})}
if(req.method==='POST'){const body=typeof req.body==='string'?JSON.parse(req.body):req.body||{};const businessId=clean(body.businessId);if(!businessId||!await requireAdmin(token,businessId))return json(res,403,{ok:false,error:'Not authorized.'});const old=await rest(`/rest/v1/nona_me_integrations?business_id=eq.${encodeURIComponent(businessId)}&select=telegram_bot_token,telegram_chat_id`);const oldToken=clean(old.data?.[0]?.telegram_bot_token);const botToken=clean(body.botToken)||oldToken;const chatId=clean(body.chatId);if(!botToken||!chatId)return json(res,400,{ok:false,error:'Bot Token and Group / Channel ID are required.'});const up=await rest(`/rest/v1/nona_me_integrations?on_conflict=business_id`,{method:'POST',headers:{prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify({business_id:businessId,telegram_bot_token:botToken,telegram_chat_id:chatId,updated_at:new Date().toISOString()})});if(!up.res.ok)return json(res,500,{ok:false,error:up.raw||'Could not save Telegram settings.'});return json(res,200,{ok:true,settings:{configured:true,chatId,maskedToken:'••••••••••••••••'}})}return json(res,405,{ok:false,error:'Use GET or POST.'})}catch(e){return json(res,500,{ok:false,error:e?.message||String(e)})}}
